# Raycast GemAI - Универсальное AI расширение

**raycast-gemai** - Продакшен расширение Raycast для AI-обработки текста с универсальной поддержкой Gemini/OpenAI.

## 🚫 КРИТИЧНЫЕ ПРАВИЛА

### 🔧 ИЗМЕНЕНИЕ ПРАВИЛ (ОБЯЗАТЕЛЬНО!)
**🚨 КРИТИЧЕСКИ ВАЖНО: При запросе на изменение правил - ПРИМЕНЯТЬ в директориях .cursor/!**

**Правила изменения правил:**
- **При запросе "изменить правила"** → ОБЯЗАТЕЛЬНО применять изменения в файлах `.cursor/rules/*.mdc`
- **При запросе "добавить правило"** → ОБЯЗАТЕЛЬНО создавать/редактировать файлы в `.cursor/rules/`
- **При запросе "обновить правила"** → ОБЯЗАТЕЛЬНО применять в директориях `.cursor/`
- **НЕ предлагать** копировать в UI Cursor - ПРИМЕНЯТЬ НАПРЯМУЮ в файлы
- **Использовать `edit_file`** для внесения изменений в `.mdc` файлы
- **Обновлять структуру** в таблице файлов при добавлении новых правил

### 🚫 Политика Git коммитов (СТРОГО!)
- **Коммиты делать ТОЛЬКО если пользователь ЯВНО попросил**
- **НИКОГДА не делать коммиты самостоятельно без явного запроса**
- **🚫 НИКОГДА НЕ ДЕЛАТЬ `git push` БЕЗ ЯВНОЙ КОМАНДЫ ПОЛЬЗОВАТЕЛЯ!**
- **Тексты коммитов ТОЛЬКО на русском языке** - никаких английских сообщений
- **Детальные коммиты**: Указывай что именно изменено и в каких файлах
- Формат коммитов: `тип: детальное описание изменений`
- Примеры: 
  - `feat: добавлена поддержка GPT-4o в buildOpenAIConfig.ts и обновлены цены в models.ts`
  - `fix: исправлена ошибка подсчёта токенов в aiProvider.ts для reasoning моделей`

### 🚫 Политика NPM команд (СТРОГО!)
- **🚫 НИКОГДА НЕ ЗАПУСКАТЬ `npm run dev` в проекте raycast-gemai**
- **🚫 НИКОГДА НЕ ЗАПУСКАТЬ `npm run build` в проекте raycast-gemai**
- **🚫 НИКОГДА НЕ ЗАПУСКАТЬ любые другие npm команды без явного разрешения**
- **ТОЛЬКО редактировать код** без запуска каких-либо npm команд
- **ВСЕГДА предлагать** пользователю запустить команды если это необходимо
- **Формат предложения**: "Рекомендую запустить `npm run build` для проверки сборки"
- **НЕ выполнять команды автоматически** - только предлагать

### 📚 СИНХРОНИЗАЦИЯ README ФАЙЛОВ (ОБЯЗАТЕЛЬНО!)
**🚨 КРИТИЧЕСКИ ВАЖНО: При любых изменениях кода синхронизировать ВСЕ README файлы!**

**Многоязычные README файлы проекта:**
- `README.md` - Основной английский
- `README_RUS.md` - Русская версия  
- `README_CN.md` - Китайская версия

**Правила синхронизации:**
- **Любое изменение функциональности** → обновить ВСЕ три README файла
- **Новые команды/функции** → добавить во все языки с соответствующими переводами
- **Изменения в конфигурации** → отразить во всех версиях
- **Обновления моделей/цены** → синхронизировать таблицы во всех файлах
- **Изменения в установке/настройке** → обновить все инструкции

**Порядок обновления:**
1. **Сначала** обновить основной `README.md` (английский)
2. **Затем** синхронизировать `README_RUS.md` (русский)
3. **Наконец** обновить `README_CN.md` (китайский)
4. **Проверить** что все изменения отражены гармонично

## 🏗️ АРХИТЕКТУРА И ПРОВАЙДЕРЫ

### Универсальная система провайдеров
- **ВСЕГДА используй `buildAIConfig()`** вместо `buildGemAIConfig()` для нового кода
- `AIConfig` - универсальный интерфейс, `GemAIConfig` только для обратной совместимости
- Автоопределение провайдера на базе выбранной модели в `buildAIConfig.ts`
- Паттерн фабрики: `createAIProvider(config)` возвращает подходящий провайдер

### Структура ключевых файлов
```
src/core/
  ├── aiProvider.ts          # Универсальные реализации AI провайдеров
  ├── buildAIConfig.ts       # Универсальный роутер конфигов (ИСПОЛЬЗУЙ ЭТО)
  ├── buildGemAIConfig.ts    # Устаревший Gemini конфиг (поддерживай совместимость)
  ├── buildOpenAIConfig.ts   # Конструктор OpenAI конфига
  ├── types.ts               # Универсальные типы и интерфейсы
  ├── models.ts              # Определения моделей с ценами/возможностями
  └── gemai.tsx              # Основной UI компонент (универсальный)
```

## 🤖 AI МОДЕЛИ И ПРОВАЙДЕРЫ

### Reasoning модели (o1-серия) - КРИТИЧНО!
- **НИКОГДА** не отправляй system prompts как отдельные сообщения в o1-preview/o1-mini
- **ВСЕГДА** включай system prompt в user message для reasoning моделей
- Используй `max_completion_tokens` вместо `max_tokens` для o1-серии
- Фиксированная температура = 1 для reasoning моделей (требование OpenAI)

### Vision API автопереключение - КРИТИЧНО!
- **Reasoning модели НЕ поддерживают vision** - автопереключение o1 → GPT-4o при обнаружении изображения
- Показывай пользователю уведомление об автопереключении модели
- Сохраняй исходные настройки конфига при переключении
- Используй `switchToVisionModel()` в `aiProvider.ts`

### Актуальные цены моделей (проверяй регулярно!)

**OpenAI:**
- **GPT-4o**: $2.50/$10.00 (ввод/вывод за 1M токенов)
- **GPT-4o-mini**: $0.15/$0.60  
- **o1-preview**: $15.00/$60.00
- **o1-mini**: $3.00/$12.00

**Gemini:**
- **Gemini 1.5 Pro**: $1.25/$5.00
- **Gemini 1.5 Flash**: $0.075/$0.30

### Подсчет токенов и статистика
- Используй реальные данные использования API (`usageMetadata`) когда доступно
- OpenAI: `prompt_tokens` включает system + user, вычисляй user токены вычитанием
- Gemini: `promptTokenCount` = весь ввод, `candidatesTokenCount` = выходные токены
- Включай thinking токены для reasoning моделей в статистику
- **Точные формулы**: (input_tokens * input_price + output_tokens * output_price) / 1M

## 💻 СТАНДАРТЫ РАЗРАБОТКИ

### TypeScript и архитектура
- **Используй строгий TypeScript** - включи все флаги строгого режима
- **Предпочитай интерфейсы типам** для форм объектов
- **Используй литеральные типы** для имен моделей, типов провайдеров: `'gemini' | 'openai'`
- **Избегай `any`** - используй `unknown` или правильные type guards

### Паттерны дизайна интерфейсов
```typescript
// ✅ Универсальный интерфейс с расширениями провайдеров
interface AIConfig {
  provider: 'gemini' | 'openai';
  model: { /* универсальные поля */ };
}

// ✅ Обратная совместимость через расширение
interface GemAIConfig extends AIConfig {
  provider: 'gemini';
  // обязательные поля специфичные для провайдера
}
```

### Raycast интеграция
- **Экспортируй default функцию** для каждой команды
- **Используй тип RaycastProps** для типизации пропсов
- **Обрабатывай launchContext** для интеграции скриншотов
- **Консистентные UI паттерны** между командами

### Шаблон структуры команды
```typescript
export default function MyCommand(props: RaycastProps) {
  const aiConfig = buildAIConfig(getCmd(CMD_MY_COMMAND).id, props, fallbackPrompt);
  aiConfig.ui.placeholder = "Кастомный плейсхолдер...";
  return GemAI(aiConfig);
}
```

## 🔧 ОТЛАДКА И ПРОИЗВОДИТЕЛЬНОСТЬ

### Политика отладочного логирования - ПРОДАКШЕН СТАНДАРТЫ
- **Продакшен**: Все отладочные `console.log()` должны быть закомментированы
- **Сохраняй обработку ошибок**: `console.error()` для настоящих ошибок должны остаться
- Формат комментариев: `// console.log(...)` для сохранения отладочного кода
- **НИКОГДА** не удаляй отладочный код - только комментируй

### Обработка ошибок
- **Специфичные типы ошибок**: `AIProviderError`, не обобщенный `Error`
- **Пользователе-дружественные сообщения**: Четкие объяснения для сбоев API
- **Плавная деградация**: Обрабатывай отсутствующие ключи, лимиты скорости, сетевые проблемы
- **Toast уведомления**: Подходящие стили (Success/Failure/Animated)

### Измерение производительности
- **Время первого ответа**: От начала запроса до первого чанка контента
- **НИКОГДА** не измеряй время до начала стриминга - это инициализация
- Общее время: полная продолжительность стриминга + финальная обработка

## 🖼️ СКРИНШОТЫ И VISION

### Обработка изображений
- OpenAI: base64 кодирование для vision моделей
- Gemini: upload через Files API с проверкой состояния
- Поддерживаемые форматы: PNG, JPEG, WebP, GIF
- Проверяй размер файла и MIME тип

### Паттерн скриншотов
```typescript
// ✅ Интеграция с launchContext
if (props.launchContext?.attachmentFile) {
  const screenshotPath = props.launchContext.attachmentFile;
  const attachment = await provider.prepareAttachment(screenshotPath);
}
```

### Команды скриншотов
- `screenshotToMarkdown`: преобразование скриншота → markdown
- `screenshotToTranslate`: перевод текста со скриншота
- `screenshotToExplain`: объяснение содержимого скриншота
- Все команды поддерживают автопереключение моделей для vision

## 🔄 ОБРАТНАЯ СОВМЕСТИМОСТЬ UI/UX (КРИТИЧНО!)
**🚨 КРИТИЧЕСКИ ВАЖНО: Обратная совместимость ОБЯЗАТЕЛЬНА для пользователей!**

### Принципы обратной совместимости:
- **НИКОГДА не ломать существующий UI/UX** - пользователи должны продолжать работать как привыкли
- **Новые функции должны быть дополнительными**, а не заменяющими
- **Сохранять все существующие настройки и предпочтения** пользователей
- **Не изменять существующие горячие клавиши** без крайней необходимости
- **Тестировать изменения** с точки зрения пользователя, а не разработчика

### Критичные тестовые сценарии
1. Скриншот + OpenAI: Проверь что screenshotToMarkdown работает с GPT-4o
2. Reasoning + Vision: Тестируй автопереключение с o1-mini на GPT-4o с изображениями
3. Статистика токенов: Проверяй точный подсчет токенов для обоих провайдеров
4. Цены моделей: Убедись что расчет стоимости соответствует официальным ценам API

**Это продакшен расширение Raycast. Приоритет: стабильность + UX.** 