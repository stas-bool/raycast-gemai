# Raycast GemAI - Правила

✅ **ОСНОВНЫЕ ПРАВИЛА ПЕРЕНЕСЕНЫ** в `.cursor/rules/*.mdc` для лучшей организации.

## 📁 Структура правил

| Файл | Содержание |
|------|------------|
| **001-workspace.mdc** | Архитектура проекта, универсальная система провайдеров |
| **002-cursor-rules.mdc** | Политика Git, структура .mdc файлов |
| **100-typescript-raycast.mdc** | Стандарты TypeScript и Raycast интеграции |
| **200-ai-provider-patterns.mdc** | Паттерны AI провайдеров, reasoning модели |
| **201-models-pricing.mdc** | Определения моделей и управление ценами |
| **202-debugging-troubleshooting.mdc** | Стратегии отладки, логирование |
| **203-screenshot-vision.mdc** | Vision API, обработка скриншотов |

## 🚫 КРИТИЧНЫЕ ПРАВИЛА

### Политика Git (СТРОГО!)
- **Коммиты ТОЛЬКО** при команде "коммит"/"commit" от пользователя
- **НИКОГДА** не коммитить самостоятельно
- **НИКОГДА** не делать `git push` без явной команды
- **Тексты коммитов ТОЛЬКО на русском**

### Универсальная система
- **ВСЕГДА** используй `buildAIConfig()` + `createAIProvider()`
- **НИКОГДА** `buildGemAIConfig()` для нового кода
- Автоопределение провайдера по модели

### Продакшен стандарты
- Комментируй отладочные `console.log()`, сохраняй `console.error()`
- Reasoning модели: system prompt в user message, `max_completion_tokens`
- Vision: автопереключение o1 → GPT-4o для изображений

**Это продакшен расширение Raycast. Приоритет: стабильность + UX.** 