---
description: 
globs: 
alwaysApply: true
---
# Стандарты разработки TypeScript и Raycast

## 🚫 Политика Git коммитов (КРИТИЧНО)
- **Коммиты делать ТОЛЬКО если пользователь ЯВНО попросил**
- **НИКОГДА не делать коммиты самостоятельно**
- **🚫 НИКОГДА НЕ ДЕЛАТЬ `git push` БЕЗ ЯВНОЙ КОМАНДЫ ПОЛЬЗОВАТЕЛЯ!**
- **Тексты коммитов ТОЛЬКО на русском языке**
- **Детальные коммиты**: Указывай конкретные изменения и файлы
- Формат: `тип: детальное описание изменений с указанием файлов`

## Руководящие принципы TypeScript
- **Используй строгий TypeScript** - включи все флаги строгого режима
- **Предпочитай интерфейсы типам** для форм объектов
- **Используй литеральные типы** для имен моделей, типов провайдеров: `'gemini' | 'openai'`
- **Избегай `any`** - используй `unknown` или правильные type guards
- **Дженерик ограничения** для провайдер-агностических функций

## Паттерны дизайна интерфейсов
```typescript
// ✅ Универсальный интерфейс с расширениями провайдеров
interface AIConfig {
  provider: 'gemini' | 'openai';
  model: { /* универсальные поля */ };
}

// ✅ Обратная совместимость через расширение
interface GemAIConfig extends AIConfig {
  provider: 'gemini';
  // обязательные поля специфичные для провайдера
}
```

## Стандарты интеграции Raycast
- **Экспортируй default функцию** для каждой команды
- **Используй тип RaycastProps** для типизации пропсов
- **Обрабатывай launchContext** для интеграции скриншотов
- **Консистентные UI паттерны** между командами

## Шаблон структуры команды
```typescript
export default function MyCommand(props: RaycastProps) {
  const aiConfig = buildAIConfig(getCmd(CMD_MY_COMMAND).id, props, fallbackPrompt);
  aiConfig.ui.placeholder = "Кастомный плейсхолдер...";
  return GemAI(aiConfig);
}
```

## Обработка ошибок
- **Специфичные типы ошибок**: `AIProviderError`, не обобщенный `Error`
- **Пользователе-дружественные сообщения**: Четкие объяснения для сбоев API
- **Плавная деградация**: Обрабатывай отсутствующие ключи, лимиты скорости, сетевые проблемы
- **Toast уведомления**: Подходящие стили (Success/Failure/Animated)

## Асинхронные паттерны
- **Используй async/await** консистентно
- **Паттерн фабрики провайдеров**: `createAIProvider(config)`
- **Потоковые ответы**: Правильная обработка AsyncGenerator
- **Проброс контекста**: Поддерживай конфиг через асинхронные цепочки

## Организация файлов
- Держи логику отделенной от Raycast-специфичного кода
- Универсальные функции провайдеров работают с Gemini и OpenAI
- Четкая типизация параметров для конфигураций моделей
- Аннотации возвращаемых типов для сложных функций
