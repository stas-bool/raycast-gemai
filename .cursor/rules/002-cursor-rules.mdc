---
description: 
globs: 
alwaysApply: false
---
# Структура .mdc файлов и лучшие практики

## Конвенция именования файлов
- Формат: `NNN-описательное-имя.mdc` (NNN = 001-299)
- 001-099: Основные/рабочие правила
- 100-199: Правила интеграции  
- 200-299: Правила для конкретных паттернов/функций
- Файлы с большими номерами имеют приоритет при конфликте правил

## Компоненты MDC файлов

### 1. Frontmatter (YAML)
```yaml
---
description: Краткое описание назначения (появляется в контексте AI)
globs: запятая,разделенные,glob,паттерны
alwaysApply: true|false
---
```

### 2. Основное содержание
- Держи под 25 строк для оптимальной эффективности токенов
- Используй краткую/сокращенную нотацию
- Помечай файлы с @ для перекрестных ссылок
- Фокусируйся на ключевых директивах и ограничениях

## Типы правил

| Тип | Описание | Настройки Frontmatter |
|-----|----------|----------------------|
| **Always** | Всегда включено в контекст модели | `alwaysApply: true`, `globs:` опционально |
| **Auto Attached** | Включается когда файлы соответствуют glob паттерну | `alwaysApply: false`, `globs: pattern` |
| **Agent Requested** | AI решает когда включать на основе описания | `description: детальное`, `globs:` пусто |
| **Manual** | Только при явной ссылке с @ruleName | минимальный frontmatter |

## Лучшие практики
- Используй @ теги для ссылок на файлы: `@buildAIConfig.ts`
- Держи правила сфокусированными и применимыми
- Избегай дублирования между файлами правил
- Тестируй эффективность правил с соответствующими паттернами файлов
- Обновляй через UI Cursor, не прямым редактированием файлов

## 🚫 Политика Git коммитов (КРИТИЧНО)
- **Коммиты делать ТОЛЬКО если пользователь ЯВНО попросил**
- **НИКОГДА не делать коммиты самостоятельно без явного запроса**
- **🚫 НИКОГДА НЕ ДЕЛАТЬ `git push` БЕЗ ЯВНОЙ КОМАНДЫ ПОЛЬЗОВАТЕЛЯ!**
- **Тексты коммитов ТОЛЬКО на русском языке**
- **Детальные описания**: Указывай изменения и затронутые файлы

**Примечание**: Помощник может предложить изменения в .mdc файлы для копирования пользователем в UI. Помощник не должен пытаться напрямую редактировать .mdc файлы.
